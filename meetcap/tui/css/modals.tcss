StopConfirmModal, DeleteConfirmModal, ReprocessModal, ErrorModal {
    align: center middle;
}

#stop-confirm-dialog, #delete-confirm-dialog, #reprocess-dialog, #error-dialog {
    width: 60;
    height: auto;
    padding: 2;
    background: $panel;
    border: thick $primary;
}

#stop-confirm-title, #delete-confirm-title, #reprocess-title, #error-title {
    text-style: bold;
    text-align: center;
    padding: 0 0 1 0;
    color: $primary;
}

#stop-confirm-body, #delete-confirm-body, #reprocess-body, #error-message {
    padding: 0 0 1 0;
    text-align: center;
}

#error-suggestion {
    padding: 0 0 1 0;
    color: $warning;
    text-align: center;
}

#stop-confirm-buttons, #delete-confirm-buttons, #reprocess-buttons {
    layout: horizontal;
    height: auto;
    align: center middle;
}

#stop-confirm-buttons Button, #delete-confirm-buttons Button, #reprocess-buttons Button {
    margin: 0 1;
    min-width: 16;
}

#btn-close-error {
    margin: 1 0 0 0;
    width: 100%;
}
